[build]
target = "index.html"
dist = "dist"
minify = "on_release"
watch = ["src", "public", "index.html"]

# Configurazione per GitHub Pages
[serve]
port = 8080

# Imposta il percorso base per GitHub Pages
[build.release]
public_url = "/wasm-image-studio-rs/"